CREATE TABLE Passenger (
    ssn INTEGER PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    address VARCHAR(255),
    city VARCHAR(50),
    county VARCHAR(50),
    phone2 VARCHAR(15),
    bdate DATE
);
