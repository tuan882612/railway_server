CREATE TABLE Train (
    train_no INTEGER PRIMARY KEY,
    name VARCHAR(50),
    premium_fare INTEGER,
    general_fare INTEGER,
    source VARCHAR(50),
    destination VARCHAR(50),
    available DATE
);
